export default function App() {
  return (
    <main className="mx-auto max-w-3xl p-8">
      <h1 className="text-2xl font-semibold text-slate-900">Spell catalog</h1>
      <p className="mt-2 text-slate-600">
        Fill <code className="rounded bg-slate-100 px-1">SPEC.md</code> before
        you generate. The familiar&apos;s first attempt lives in{" "}
        <code className="rounded bg-slate-100 px-1">familiar-output/</code>.
      </p>
    </main>
  );
}
