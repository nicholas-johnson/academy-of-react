// TODO: Build a Modal that wraps any content with the children prop.
// Follow the three tasks in the README — CSS classes are already in App.css.
function Modal({ isOpen, onClose, title, children }) {
  return null;
}

export default Modal;
