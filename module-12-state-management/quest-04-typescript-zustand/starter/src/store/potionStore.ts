// TODO: Import create from "zustand"
// TODO: Import persist from "zustand/middleware"
// TODO: Import your PotionState type from "../types/potion"

// TODO: Create the store using the typed, curried pattern:
//
// export const usePotionStore = create<PotionState>()(
//   persist(
//     (set, get) => ({
//       potions: [],
//       categoryFilter: "all",
//
//       addPotion: (potion) => ...,
//       removePotion: (id) => ...,
//       setCategoryFilter: (category) => ...,
//       getTotalValue: () => ...,
//     }),
//     { name: "potion-ledger" }
//   )
// )

// Placeholder so the starter compiles — remove when you implement the real store
export const usePotionStore = null;
